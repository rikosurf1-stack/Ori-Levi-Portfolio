# Netlify deployment configuration.
# The official Next.js plugin handles the build output automatically.

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
